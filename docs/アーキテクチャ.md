# アーキテクチャ

## 1. システム全体構成

```mermaid
graph TB
    subgraph Browser["ブラウザ"]
        UI0["ログイン画面<br/>(login.html)"]
        UI1["TODOリスト<br/>(Alpine.js)"]
        UI2["ジョブ管理画面<br/>(Alpine.js)"]
    end

    subgraph Docker["Docker Compose"]
        subgraph API["api コンテナ (FastAPI)"]
            Auth["Routers<br/>/api/auth"]
            Router["Routers<br/>/api/todos<br/>/api/jobs<br/>/api/jobs/stream"]
            Deps["core/deps.py<br/>JWT認証"]
            Service["Services<br/>ビジネスロジック"]
            Static["Static Files<br/>login.html / index.html / jobs.html"]
        end

        subgraph Worker["worker コンテナ (Taskiq)"]
            TaskFn["タスク関数<br/>run_sample_job()"]
        end

        Redis[("Redis<br/>タスクキュー<br/>+ 結果ストア")]
        DB[("SQLite<br/>dev.db")]
    end

    UI0 -- "POST /api/auth/login" --> Auth
    Auth -- "JWT発行" --> UI0
    UI1 -- "REST (CRUD) + Bearer" --> Router
    UI2 -- "REST + SSE (?token=)" --> Router
    Router --> Deps
    Deps --> Service
    Service --> DB
    Router -- "broker.kiq()" --> Redis
    Redis -- "タスク取得" --> TaskFn
    TaskFn -- "update_job_status()" --> DB
```

---

## 2. レイヤー構成

```mermaid
graph LR
    subgraph Frontend["フロントエンド (static/)"]
        HTML["HTML + Alpine.js"]
        AppJS["app.js<br/>authFetch / getToken<br/>テーマ管理"]
    end

    subgraph API["アプリケーション層 (app/)"]
        R["routers/\nルーティング・バリデーション"]
        D["core/deps.py\nJWT認証"]
        S["services/\nビジネスロジック"]
        M["models/\nDBテーブル定義"]
        SC["schemas/\nAPI入出力型"]
    end

    subgraph Infra["インフラ層"]
        DB2[("SQLite / PostgreSQL")]
        RD[("Redis")]
    end

    HTML -->|"authFetch / EventSource"| R
    AppJS -.->|"共通ユーティリティ"| HTML
    R --> D
    D -->|"Bearer / ?token="| R
    R --> SC
    R --> S
    S --> M
    M --> DB2
    R -->|"broker.kiq()"| RD
    RD -->|"タスク受信"| T["tasks/\nTaskiqタスク関数"]
    T --> S
```

---

## 3. ジョブのライフサイクル

```mermaid
sequenceDiagram
    actor User as ユーザー
    participant UI as jobs.html<br/>(Alpine.js)
    participant API as FastAPI
    participant DB as SQLite
    participant Redis
    participant Worker as Taskiqワーカー

    User->>UI: ジョブ投入ボタン押下
    UI->>API: POST /api/jobs {name, params}
    API->>DB: Job作成 (status=pending)
    API->>Redis: タスクをキューに追加 (broker.kiq)
    API-->>UI: 201 Created {id, status=pending}

    loop SSE (1秒ごと)
        UI->>API: GET /api/jobs/stream
        API->>DB: ジョブ一覧取得
        API-->>UI: data: [{status, progress, ...}]
    end

    Worker->>Redis: タスク取得
    Worker->>DB: status=running, started_at=now
    loop 1秒ごと（N回）
        Worker->>DB: progress更新
        Worker->>DB: キャンセル確認
    end
    Worker->>DB: status=success, result=..., finished_at=now

    UI-->>User: 進捗バー・ステータスがリアルタイム更新
```

---

## 4. キャンセルフロー

```mermaid
sequenceDiagram
    actor User as ユーザー
    participant UI as jobs.html
    participant API as FastAPI
    participant DB as SQLite
    participant Worker as Taskiqワーカー

    User->>UI: キャンセルボタン押下
    UI->>API: DELETE /api/jobs/{id}
    API->>DB: status=cancelled, finished_at=now
    API-->>UI: 204 No Content

    Note over Worker: 次のループで DB をチェック
    Worker->>DB: Job取得 → status=cancelled を検知
    Worker-->>Worker: 処理を中断してreturn
```

---

## 5. ディレクトリ構成

```
fastapi/
├── app/
│   ├── main.py          # アプリエントリーポイント・ルーター登録
│   ├── broker.py        # Taskiqブローカー（Redis接続）
│   ├── database.py      # SQLModelエンジン・get_session
│   ├── core/
│   │   └── deps.py      # JWT認証依存関係（get_current_user）
│   ├── models/
│   │   ├── user.py      # User テーブル
│   │   ├── todo.py      # Todo テーブル（user_id FK）
│   │   └── job.py       # Job テーブル + JobStatus enum（user_id FK）
│   ├── schemas/
│   │   ├── auth.py      # LoginRequest / Token / UserRead
│   │   ├── todo.py      # TodoCreate / TodoRead / TodoUpdate
│   │   └── job.py       # JobCreate / JobRead
│   ├── routers/
│   │   ├── auth.py      # /api/auth エンドポイント
│   │   ├── todo.py      # /api/todos エンドポイント
│   │   └── job.py       # /api/jobs エンドポイント + SSE + TASK_REGISTRY
│   ├── services/
│   │   ├── auth.py      # bcryptハッシュ・JWT生成/検証・ユーザー認証
│   │   ├── todo.py      # TODO CRUD
│   │   └── job.py       # Job CRUD + ステータス更新
│   └── tasks/
│       └── sample.py    # デモタスク（進捗更新・キャンセル対応）
├── scripts/
│   └── create_user.py   # ユーザー登録CLIスクリプト
├── static/
│   ├── login.html       # ログイン UI
│   ├── index.html       # TODOリスト UI
│   ├── jobs.html        # ジョブ管理 UI
│   └── app.js           # 共通ユーティリティ（authFetch / テーマ）
├── tests/
│   ├── conftest.py      # pytest フィクスチャ
│   ├── test_auth.py     # 認証APIテスト（9ケース）
│   ├── test_jobs.py     # ジョブAPIテスト（11ケース）
│   └── test_todos.py    # TODO APIテスト（6ケース）
├── docs/                # 設計ドキュメント
├── docker-compose.yml   # redis / api / worker
├── Dockerfile
├── pyproject.toml
└── .env.example
```

---

## 6. 技術スタック

| 区分 | 技術 | バージョン | 用途 |
|---|---|---|---|
| Web フレームワーク | FastAPI | ≥0.115 | REST API・SSE・静的ファイル配信 |
| ORM / スキーマ | SQLModel | ≥0.0.21 | DBモデル定義・バリデーション |
| 認証 | PyJWT + bcrypt | ≥2.8 / ≥4.0 | JWT生成/検証・パスワードハッシュ |
| タスクキュー | Taskiq | ≥0.11 | 非同期ジョブ管理 |
| キューブローカー | taskiq-redis | ≥1.0 | Redis キュー・結果ストア |
| FastAPI 連携 | taskiq-fastapi | ≥0.3 | ブローカーのライフサイクル管理 |
| DB | SQLite（開発） / PostgreSQL（本番） | — | データ永続化 |
| キャッシュ/キュー | Redis | 7 | タスクキュー・結果バックエンド |
| フロントエンド | Alpine.js + DaisyUI | 3 / 4 | リアクティブ UI（ビルド不要） |
| パッケージ管理 | uv | — | 依存解決・仮想環境 |
| コンテナ | Docker Compose | — | ローカル開発環境 |
| テスト | pytest + httpx + pytest-cov | ≥8.0 | API テスト・カバレッジ計測 |
| Lint | Ruff | ≥0.7 | コード品質管理 |

---

## 7. スケールアップ時の移行方針

### DB を PostgreSQL へ移行

`.env` の `DATABASE_URL` を変更するだけで動作する（SQLModel が吸収）。

```
DATABASE_URL=postgresql+psycopg2://user:password@host:5432/dbname
```

### ワーカーをスケールアウト

`docker compose` でワーカーのレプリカ数を増やす：

```bash
docker compose up --scale worker=4
```

### ジョブ種別の追加

`app/tasks/` にタスク関数を追加し、`TASK_REGISTRY` に登録するだけでAPIから利用可能になる（詳細は[仕様書](仕様書.md#4-新しいジョブ種別の追加方法)参照）。
